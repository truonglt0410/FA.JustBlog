@model IEnumerable<Tag>

<h3>Tag Clouds</h3>

@foreach (var item in Model)
{
    <a href="@Url.Action("Details", "Tags", new { urlSlug = item.UrlSlug})" class="btn rounded-pill bg-info mb-2">
        @item.Name
    </a>
}